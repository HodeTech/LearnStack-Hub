{
  "name": "learnstack-hub-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "LearnStack Hub operator-facing frontend monorepo (operator portal Next.js app per ADR-0019).",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "dev": "pnpm --filter @learnstack-hub/operator-portal dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "prettier": "3.3.3"
  }
}
