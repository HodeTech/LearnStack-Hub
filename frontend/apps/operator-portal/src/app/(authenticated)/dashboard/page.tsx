export default function DashboardPage() {
  return (
    <section className="mx-auto max-w-5xl px-6 py-12">
      <h1 className="text-3xl font-semibold">Operator dashboard</h1>
      <p className="mt-4 text-hub-muted">
        P02c-0 placeholder. P02c-4 wires the real dashboard: tenant count by
        plan, custom-domain pipeline state, license-verify success rate,
        entitlement projection push lag.
      </p>
    </section>
  );
}
