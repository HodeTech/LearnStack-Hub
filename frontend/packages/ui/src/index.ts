// Operator-portal UI primitives. P02c-0 ships an empty package; P02c-4 fills
// it with the operator-portal design system primitives (tenant list table,
// plan editor, entitlement viewer, custom-domain status pill, license-key
// download card, audit log row).
//
// Phase 11 may consolidate this with a shared design system across the two
// frontends (operator-portal here + LearnStack core's apps/web); for now the
// duplication is intentional — Hub's aesthetic is operator-tool, LearnStack's
// is per-tenant brand-theming.
export {};
